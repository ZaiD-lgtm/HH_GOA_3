# samples/

Drop the input face scan here, e.g. `samples/probe.jpg`, then:

```
hhg3 run --image samples/probe.jpg
```

Images in this folder are gitignored on purpose — do not commit photographs of
real people to a public repo. `fixtures.json` feeds the offline `mock` search
provider and contains no personal data beyond a public Wikimedia URL.
